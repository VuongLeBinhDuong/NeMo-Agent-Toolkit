Create a food store homepage: header (logo/menu/search/cart with count + total), hero (banner + CTA), 8-product grid “Add to Cart”. Plain HTML/CSS/JS only; hardcode products in HTML; search by name, category filter (All/BBQ/Stir-fry/Soup/Dessert), sort (popular/price asc/desc), responsive layout, add “HOT/NEW” badges on product cards.

Create a simple clothing store homepage with a modern, minimal aesthetic. The header should include a left-aligned store logo, navigation links for Men, Women, Kids, and Accessories, a centered search bar that filters products as the user types, and a cart summary on the right showing item count and subtotal. Add a large hero banner promoting the current seasonal collection, such as “Fall Essentials” or “New Arrivals,” with a call-to-action button. Below the banner, display a featured product grid of 10–12 items, including examples like Men’s Denim Jacket, Women’s Linen Dress, Kids’ Hoodie, Basic T-Shirt, Running Shoes, Tote Bag, and Sunglasses. Each product should show a name, price, small description, image placeholder, and optional labels like “New,” “Sale,” or “Best Seller.” Include a category filter, a sorting dropdown (price low-to-high, high-to-low, popularity), and a toggle to show only discounted items. Clicking “Add to Cart” should update the cart summary instantly. The layout must be clean, spacious, and fully responsive, with neutral color tones and subtle shadows that match a minimalist fashion brand feel.

Prompt 1 – Tech Gadget Store

Create a multi-page Tech Gadget Store website using only HTML, CSS, and vanilla JavaScript. Include a homepage with a hero banner, featured products, and a responsive layout that adjusts from three columns to one. Add a shop page with category filtering, sorting options, and live search. Add a product detail page with images, specifications, badges, and an Add to Cart button. Add a cart page showing items stored in localStorage with quantity controls and totals. All pages must share the same responsive header with logo, menu, search bar, and a cart section showing item count and subtotal, as well as a footer. Products must be hardcoded directly in HTML, and all functionality must work without using any icons or special symbols.

Prompt 2 – Organic Grocery Shop

Build a modern organic grocery homepage using plain HTML/CSS/JS only. It should include a sticky header with the store logo, navigation links, a live search bar, and a cart summary showing total items and cost. The hero section should display a wide banner promoting “Fresh & Local” products with a “Shop Now” button. Below it, create a product grid of 10 items (categories: Fruits, Vegetables, Beverages, Bakery). Add a dropdown to filter by category, another dropdown to sort by price or popularity, and a toggle to show only “Discounted” items. When users click “Add to Cart,” update the cart count and total instantly. The design should be fully responsive and have a clean, eco-themed aesthetic.

Prompt 3 – Home Decor & Furniture Store

Design a home decor and furniture store landing page using pure HTML, CSS, and JavaScript (no libraries). Include a header (logo, navigation menu, search bar, category filter, cart counter), a large hero image with a promotional message, and a responsive grid displaying 12 furniture/decor products. Each card must show an image, product name, price, and an “Add to Cart” button. Implement client-side filtering by type (All/Furniture/Lighting/Decor) and sorting options (popular, price ascending, price descending). Add “HOT” and “TRENDING” badges on select items. Ensure that the product grid adapts for desktop, tablet, and mobile layouts, with smooth hover effects.

Prompt 4 – Online Pet Supply Store

Create a pet supply shop homepage using HTML, CSS, and JavaScript (no frameworks). It should have a top bar with logo, menu, search input, and a cart indicator showing count and total cost. The hero section should have a full-width banner featuring “Everything for Your Pet” with a CTA button. Below, make a responsive grid of 8 products (categories: Dog, Cat, Bird, Fish). Each product card should include an image, name, price, description, and “Add to Cart” button. Allow users to filter products by category, search by keyword, and sort by featured or price. Show “NEW” or “BESTSELLER” tags on some cards. The layout should be clean, mobile-friendly, and visually warm (using soft tones).

Prompt 5 – Fashion & Accessories Marketplace

Build a multi-page fashion marketplace website with clean, modern styling and fully responsive behavior. The Homepage uses a fixed header styled with a compact, sticky top bar, flex layout for logo–menu–search–cart alignment, smooth hover effects, and mobile breakpoints that collapse the menu into a toggleable drawer; JavaScript controls the live search, category filtering, sorting, and a dynamic cart badge that updates in real time. The hero section uses CSS for full-width images, text overlays, and slide transitions, while JS handles the carousel timing, previous/next controls, and auto-play pauses on interaction. The product gallery uses CSS grid with adaptive columns and hover animations, and JS renders product cards dynamically, applies filters, badges, search matching, and sorting logic. The Shop page extends these CSS and JS patterns, adding grid pagination and more advanced filters (category, price range), with JS maintaining a synchronized global product state. The Product Detail page uses CSS for a responsive two-column layout with image zoom/thumbnail switching, while JS powers image preview changes, selected size/color states, and “Add to Cart” actions that update global cart storage. The Cart page or slide-out drawer uses CSS transitions for sliding animations, a structured item list layout, and sticky subtotal/checkout sections, while JS manages item quantities, total calculations, delete actions, and persistent storage through localStorage. Across all pages, CSS includes consistent typography, spacing scales, button styles, and responsive breakpoints, while JavaScript handles all interactive components, state management, DOM updates, and seamless cart synchronization.